# ARK Server Commander 环境配置示例
# 复制此文件为 .env 并填入实际值

# JWT密钥（用于用户认证）
# 生成方法: openssl rand -base64 48
# ⚠️ 必须配置，至少32字符
JWT_SECRET=PLEASE_GENERATE_A_STRONG_SECRET_USING_OPENSSL_RAND

# 数据库路径
DB_PATH=/data/ark_server.db

# 服务器端口
SERVER_PORT=8080

# Gin运行模式 (debug/release)
GIN_MODE=release

# 日志级别 (debug/info/warn/error/fatal)
LOG_LEVEL=info

# 日志格式 (json/console)
# json - 生产环境推荐，便于日志收集系统解析
# console - 开发环境推荐，彩色输出，人类可读
LOG_FORMAT=console
